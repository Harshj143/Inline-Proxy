[project]
name = "mcp-security-gateway"
version = "0.1.0"
description = "Jac-native policy and session-risk engine for the MCP Security Gateway."
entry-point = "demo/attack.jac"
jac-version = "==0.16.7"

[dependencies]
pyyaml = "==6.0.3"

[run]
diagnostics = "error"

[test]
directory = "tests"
verbose = true
